# ARCHIVED SUPABASE BACKEND

**WARNING: DO NOT USE.**

This directory contains historical, stale backend material that has been superseded by the active infrastructure located in the root `supabase/` directory.

It is kept purely for historical reference and should **not** be treated as canonical or modified.

Any active development, migrations, or edge functions must be performed in `/dinein_app/supabase/` or the root `supabase/` depending on the current monorepo structure.
